application: myappid
version: one
runtime: python27
api_version: 1
threadsafe: yes

handlers:
- url: /static
  static_dir: static

- url: /favicon\.ico
  static_files: favicon.ico
  upload: favicon\.ico

- url: /admin/.*
  script: admin.app
  login: required

- url: .*
  script: main.app

libraries:
- name: jinja2
  version: latest

inbound_services:
- warmup
